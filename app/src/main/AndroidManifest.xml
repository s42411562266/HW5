<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.hw5">

    <dist:module dist:instant="true" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Web"></activity>
        <activity android:name=".Compound"></activity>
        <activity android:name=".SeekBar2"></activity>
        <activity android:name=".Scroll"></activity>
        <activity android:name=".RatioStar"></activity>
        <activity android:name=".Men"></activity>
        <activity android:name=".Sp"></activity>
        <activity android:name=".LifeCycle"></activity>
    </application>

</manifest>